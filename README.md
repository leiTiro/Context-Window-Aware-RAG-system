# Context-Window-Aware-RAG-system
This project demonstrates a **Context-Window-Aware Retrieval-Augmented Generation (RAG)** system. Instead of sending all available information to a language model, the system deliberately assembles a bounded context window using predefined sections, strict token budgets, and explicit overflow strategies.
